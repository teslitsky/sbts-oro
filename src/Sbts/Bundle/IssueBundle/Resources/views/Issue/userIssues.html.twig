{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('sbts_issue_user_issues', { 'userId': entity.id }),
        'title': 'sbts.issue.user_issues.label' | trans
    }) }}
{% endset %}

{{ UI.scrollSubblock(null, [ widgetContent ]) }}
